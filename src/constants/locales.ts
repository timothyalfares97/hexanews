/**
 * Entry point for our locales file.
 */

import LocalesEN from './locales.en'
import LocalesID from './locales.id'

export default {
  en: {
    translations: LocalesEN
  },
  id: {
    translations: LocalesID
  }
}